generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = "file:./dev.db"
}

model Wallet {
  id          Int      @id @default(autoincrement())
  balance     Int      @default(0)
  lastClaimed DateTime?
}
